prime_cache = [2, 3, 5, 7, 11, 13, 17, 19]


def smallest_multiple(max):
    curr = 1
    while curr<=max:
        factorial = {}

        while curr > 1
            for prime in prime_cache:
                if curr % prime == 0 and curr != prime:
                    if prime in factorial.keys():
                        factorial[prime] += 1
                    else:
                        factorial[prime] = 1
            curr += 1












